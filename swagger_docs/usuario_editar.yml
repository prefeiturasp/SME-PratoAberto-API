Endpoint para realizar um PUT para atualização de um usuário
---
parameters:
  - name: email
    description: Email com dominio autorizado - @sme.prefeitura.sp.gov.br
    type: string
    required: true

  - in: body
    name: senha
    description: Senha do usuario
    required: true
responses:
  201:
    description: Mensagem de sucesso
    examples:
        {
            "HTTP": "201"
        }
